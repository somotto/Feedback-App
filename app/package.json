{
  "name": "web-api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node index.js & sleep 2 && curl -sf http://localhost:3000/healthz && kill $! || true"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/client-cloudwatch": "^3.0.0",
    "@aws-sdk/client-sns": "^3.0.0",
    "@aws-sdk/client-secrets-manager": "^3.0.0",
    "jsonwebtoken": "^9.0.0"
  }
}
